<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="clubactivity.dao.MemberDAO">
	<select id="selectMemberById" parameterType="String" resultMap="memberResultMap">
		SELECT MEMBER_ID, MEMBER_PASSWORD, MEMBER_NAME, MEMBER_PHONE_NUMBER, MEMBER_REGISTRATION_DATE, MEMBER_LEVEL_CODE, MEMBER_NUMBER
		from club.member WHERE MEMBER_ID = #{memberId}
	</select>

	<select id="selectById" parameterType="String" resultType="int">
		SELECT count(*) from club.member WHERE "MEMBER_ID" = #{memberId}
	</select>

	<insert id="insertMember" parameterType="memberRegisterRequest">
		INSERT INTO club.member (MEMBER_ID, MEMBER_PASSWORD,MEMBER_NAME, MEMBER_PHONE_NUMBER,MEMBER_REGISTRATION_DATE)
		VALUES (#{memberId}, #{memberPassword},#{memberName},#{memberPhoneNumber},now());
	</insert>

	<select id="selectMemberList" resultMap="memberListResultMap">
		select MEMBER_ID, MEMBER_NAME, MEMBER_PHONE_NUMBER, MEMBER_REGISTRATION_DATE, MEMBER_LEVEL_CODE, MEMBER_NUMBER 
		from club.member
	</select>
	
	<update id="updatePassword" parameterType="member">
		update club.member SET MEMBER_PASSWORD = #{memberPassword} where MEMBER_ID = #{memberId}
	</update>

	<resultMap type="member" id="memberListResultMap">
		<id column="MEMBER_NUMBER" property="memberNumber" />
		<result column="MEMBER_ID" property="memberId" />
		<result column="MEMBER_NAME" property="memberName" />
		<result column="MEMBER_PHONE_NUMBER" property="memberPhoneNumber" />
		<result column="MEMBER_REGISTRATION_DATE" property="memberRegisterDate" />
		<association select="selectMemberlevel"
			property="memberlevel" column="{memberLevelCode=MEMBER_LEVEL_CODE}" />
	</resultMap>

	<select id="selectMemberlevel" parameterType="memberlevel"
		resultType="memberlevel">
		SELECT MEMBER_LEVEL_CODE, MEMBER_LEVEL_DESCRIPTION 
		from club.memberlevel WHERE MEMBER_LEVEL_CODE = #{memberLevelCode}
	</select>

	<resultMap type="member" id="memberResultMap">
		<id column="MEMBER_NUMBER" property="memberNumber" />
		<result column="MEMBER_ID" property="memberId" />
		<result column="MEMBER_PASSWORD" property="memberPassword" />
		<result column="MEMBER_NAME" property="memberName" />
		<result column="MEMBER_PHONE_NUMBER"
			property="memberPhoneNumber" />
		<result column="MEMBER_REGISTRATION_DATE"
			property="memberRegisterDate" />
		<association select="selectMemberlevel"
			property="memberlevel" column="{memberLevelCode=MEMBER_LEVEL_CODE}" />
	</resultMap>

</mapper>